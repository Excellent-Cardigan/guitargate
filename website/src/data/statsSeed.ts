/**
 * Placeholder practice stats for the member app — there's no real backend yet,
 * these are seeded placeholder numbers (not derived from any other seed data)
 * for the Home streak entry point and the Stats panel. Flagging as net-new
 * placeholder content, same status as PRACTICE_STREAK_DAYS was before this.
 */
export const DAY_STREAK = 4;
export const WEEK_STREAK = 1;
export const BEST_DAY_STREAK = 12;
export const BEST_WEEK_STREAK = 3;
export const CLASSES_TAKEN = 8;
export const MINUTES_PRACTICED = 340;
